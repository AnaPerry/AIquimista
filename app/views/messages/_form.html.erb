V<%= simple_form_for [reading, message] do |f| %>
  <%= f.input :content, label: "Ask a question", required: true, placeholder: "Qual é sua dúvida?" %>
  <%= f.button :submit, 'Ask LLM', class: 'btn btn-primary' %>
<% end %>
